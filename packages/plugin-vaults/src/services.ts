const BASE_URL = "..."
const getWhitlistedProtocolsEndpoint = BASE_URL + "protocols/";

// export const getWhitlistedProtocols() {
//     try {
//         const response = await fetch(BASE_URL + "");
//     } catch (error: any) {
//         console.error("Error in Vaults API:", error.message);
//         throw error;
//     }
// }

export const createVaultsService = (provider_url: string) => {
    return;
}
